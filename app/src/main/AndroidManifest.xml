<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.getfit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Getfit">
        <activity android:name=".workoutobese"></activity>
        <activity android:name=".workoutover" />
        <activity android:name=".homepage" />
        <activity android:name=".fitwork" />
        <activity android:name=".obese" />
        <activity android:name=".overweight" />
        <activity android:name=".fit" />
        <activity android:name=".underweight" />
        <activity android:name=".userhome" />
        <activity android:name=".loginpage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>